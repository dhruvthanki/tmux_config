# Remote hosts for `prefix + Space` (scripts/remote-picker.sh).
# One SSH alias per line (must resolve via ~/.ssh/config on this machine).
# Optional second column (tab-separated) is a display label; defaults to the alias.
# Lines starting with # and blank lines are ignored.
#
# alias	label
rlpc
